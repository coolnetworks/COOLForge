# COOLForge_Lib setup config (contains encrypted API key)
.COOLForge_Lib-setup.json

# Custom field backups (may contain sensitive data)
/backups/

# Generated configuration files (contain local paths)
/config/

# Cache files (generated inventory and temporary data)
/.cache/
*.cache

# Claude Code local settings
.claude/
