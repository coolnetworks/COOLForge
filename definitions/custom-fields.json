{
  "$schema": "./custom-fields.schema.json",
  "version": "2026.01.19",
  "description": "COOLForge custom field definitions for Level.io",
  "setup": {
    "prerequisites": [
      "Level.io API key with 'Custom Fields' permission (get from https://app.level.io/api-keys)",
      "Your company/MSP name for the scratch folder path",
      "(If using private repo) GitHub Personal Access Token with 'Contents' read permission - create at github.com/settings/tokens"
    ],
    "description": "This wizard creates and configures custom fields in your Level.io account. Have the information above ready before starting.",
    "notes": [
      "NEW ACCOUNTS: If you have a new Level.io account, API access may need to be enabled by Level.io support before you can use the API. Contact support if you receive connection errors."
    ]
  },
  "fieldGroups": {
    "level_api": {
      "displayName": "Level.io API",
      "description": "These fields enable Level.io API automation (wake devices, run scripts across folders, etc.)",
      "prerequisites": [
        "A second Level.io API key for scripts to use (can be the same as setup, but recommended to create a dedicated one)"
      ]
    },
    "screenconnect": {
      "displayName": "ScreenConnect",
      "description": "These fields enable ScreenConnect (ConnectWise Control) integration for remote access management.",
      "prerequisites": [
        "Your ScreenConnect server URL (e.g., support.yourcompany.com)",
        "ScreenConnect instance ID (found in ScreenConnect admin settings) - for whitelisting scripts",
        "(Optional) ScreenConnect API credentials if using automation features"
      ]
    },
    "huntress": {
      "displayName": "Huntress",
      "description": "These fields enable automated Huntress agent deployment and management.",
      "prerequisites": [
        "Huntress account key (from Huntress dashboard > Agents > Deploy)",
        "Huntress organization key (from Huntress dashboard > Agents > Deploy)"
      ]
    },
    "unchecky": {
      "displayName": "Unchecky",
      "description": "These fields enable automated Unchecky deployment using the 5-tag policy model. Tags override custom field policy.",
      "prerequisites": [
        "Download Unchecky installer from https://www.fosshub.com/Unchecky.html",
        "Host the installer on a publicly accessible URL (S3, Azure Blob, web server, etc.)"
      ]
    },
    "technician_alerts": {
      "displayName": "Technician Alerts",
      "description": "These fields enable the technician alert notification system. Alerts appear on technician workstations tagged with the technician emoji.",
      "prerequisites": [
        "Tag technician workstations with the technician emoji tag in Level.io"
      ]
    },
    "debugging": {
      "displayName": "Debugging",
      "description": "These fields control debug output levels: normal (basics), verbose (policy decisions), veryverbose (full API trace).",
      "prerequisites": []
    },
    "policy_readme": {
      "displayName": "Policy Documentation",
      "description": "Read-only field explaining how COOLForge policies work. Always appears first in policy_ field list.",
      "prerequisites": []
    },
    "device_blocking": {
      "displayName": "Device Blocking",
      "description": "Control whether policies can make changes on a device. Defaults to 'block' for safety.",
      "prerequisites": []
    },
    "chrome": {
      "displayName": "Google Chrome",
      "description": "Chrome Enterprise software deployment and browser configuration policies.",
      "prerequisites": []
    },
    "meshcentral": {
      "displayName": "MeshCentral",
      "description": "MeshCentral agent deployment and management.",
      "prerequisites": [
        "MeshCentral server URL",
        "MeshCentral agent installer URLs for Windows, Linux, Mac"
      ]
    },
    "windows_config": {
      "displayName": "Windows Configuration",
      "description": "Windows OS-level configuration policies (location services, etc).",
      "prerequisites": []
    },
    "chrome_config": {
      "displayName": "Chrome Configuration",
      "description": "Chrome browser configuration policies (location, extensions, etc).",
      "prerequisites": []
    },
    "dnsfilter": {
      "displayName": "DNSFilter",
      "description": "DNSFilter agent deployment and management using the 5-tag policy model.",
      "prerequisites": [
        "DNSFilter site key (NKEY) from your DNSFilter portal"
      ]
    }
  },
  "fields": {
    "core": [
      {
        "name": "coolforge_msp_scratch_folder",
        "description": "Persistent storage folder for MSP scripts and libraries (default: C:\\ProgramData\\COOLForge)",
        "legacyNames": ["msp_scratch_folder"],
        "adminOnly": false,
        "required": true,
        "prompt": "Where should COOLForge store scripts and libraries on endpoints?"
      },
      {
        "name": "coolforge_ps_module_library_source",
        "description": "URL to download COOLForge-Common.psm1 library (leave empty for official repo)",
        "legacyNames": ["ps_module_library_source"],
        "adminOnly": false,
        "required": false,
        "prompt": "Custom library URL (leave empty to use official COOLForge repo)"
      },
      {
        "name": "coolforge_pin_psmodule_to_version",
        "description": "Pin scripts to a specific version tag (e.g., v2025.12.29)",
        "legacyNames": ["pin_psmodule_to_version"],
        "adminOnly": false,
        "required": false,
        "prompt": "Pin to a specific version? (leave empty for latest)"
      },
      {
        "name": "coolforge_pat",
        "description": "GitHub Personal Access Token for private repositories. Required if hosting COOLForge scripts in a private GitHub repo. Create at github.com/settings/tokens with 'repo' scope (read access). Token is stored as admin-only field so it's hidden from non-admin users.",
        "legacyNames": [],
        "adminOnly": true,
        "required": false,
        "prompt": "GitHub PAT for private repo access (leave empty if using public repo)",
        "help": "Create a fine-grained PAT at github.com/settings/tokens with read-only 'Contents' permission for your repo"
      },
      {
        "name": "coolforge_nosleep_duration_min",
        "description": "Duration in minutes to prevent device from sleeping (default: 60)",
        "legacyNames": [],
        "adminOnly": false,
        "required": false,
        "default": "60",
        "prompt": "How long should NoSleep scripts keep devices awake? (minutes)"
      }
    ],
    "level_api": [
      {
        "name": "apikey",
        "description": "Level.io API key for automation scripts (admin-only)",
        "legacyNames": [],
        "adminOnly": true,
        "required": false,
        "prompt": "Level.io API key for script automation"
      }
    ],
    "screenconnect": [
      {
        "name": "policy_screenconnect_instance_id",
        "description": "Your MSP's ScreenConnect instance ID for whitelisting",
        "legacyNames": ["screenconnect_instance_id", "screenconnect_instance", "coolforge_screenconnect_instance_id"],
        "adminOnly": true,
        "required": false,
        "prompt": "ScreenConnect instance ID (for RAT whitelisting)"
      },
      {
        "name": "policy_screenconnect_api_user",
        "description": "ScreenConnect API username for automation",
        "legacyNames": ["screenconnect_api_user"],
        "adminOnly": true,
        "required": false,
        "prompt": "ScreenConnect API username (optional)"
      },
      {
        "name": "policy_screenconnect_api_password",
        "description": "ScreenConnect API password for automation",
        "legacyNames": ["screenconnect_api_password"],
        "adminOnly": true,
        "required": false,
        "prompt": "ScreenConnect API password (optional)"
      },
      {
        "name": "policy_screenconnect_machine_hosts_screenconnect_server",
        "description": "Set to 'true' on devices hosting ScreenConnect server",
        "legacyNames": ["is_screenconnect_server", "coolforge_is_screenconnect_server", "policy_screenconnect_is_server", "policy_screenconnect_machine_hosts_screenconnect"],
        "adminOnly": false,
        "required": false,
        "default": "false"
      },
      {
        "name": "policy_screenconnect_device_url",
        "description": "Per-device ScreenConnect URL (populated automatically by scripts)",
        "legacyNames": ["screenconnect_device_url", "screenconnect_url"],
        "adminOnly": false,
        "required": false,
        "autoCreate": true
      },
      {
        "name": "policy_screenconnect",
        "description": "ScreenConnect client policy: install, remove, pin. Uses 5-tag model with SC prefix.",
        "legacyNames": [],
        "adminOnly": false,
        "required": false,
        "default": "pin",
        "valueOptions": ["pin", "install", "remove"],
        "valueFormat": "{value} | uses pin/install/remove (change to activate policy)",
        "prompt": "ScreenConnect policy (install/remove/pin)"
      },
      {
        "name": "policy_screenconnect_instance",
        "description": "ScreenConnect service display name to match/install",
        "legacyNames": [],
        "adminOnly": false,
        "required": false,
        "prompt": "ScreenConnect service display name"
      },
      {
        "name": "policy_screenconnect_baseurl",
        "description": "ScreenConnect server base URL (e.g., support.company.com)",
        "legacyNames": ["screenconnect_baseurl"],
        "adminOnly": false,
        "required": false,
        "prompt": "ScreenConnect server URL"
      }
    ],
    "huntress": [
      {
        "name": "policy_huntress",
        "description": "Huntress agent policy: install, remove, pin. Uses 5-tag model.",
        "legacyNames": [],
        "adminOnly": false,
        "required": false,
        "default": "pin",
        "valueOptions": ["pin", "install", "remove"],
        "valueFormat": "{value} | uses pin/install/remove (change to activate policy)",
        "prompt": "Huntress policy (install/remove/pin)"
      },
      {
        "name": "policy_huntress_account_key",
        "description": "Huntress account key for agent deployment",
        "legacyNames": ["huntress_account_key"],
        "adminOnly": true,
        "required": false,
        "prompt": "Huntress account key (from Dashboard > Agents > Deploy)"
      },
      {
        "name": "policy_huntress_org_key",
        "description": "Huntress organization key for agent deployment",
        "legacyNames": ["huntress_organization_key"],
        "adminOnly": true,
        "required": false,
        "prompt": "Huntress organization key (from Dashboard > Agents > Deploy)"
      },
      {
        "name": "policy_huntress_tags",
        "description": "Comma-separated tags to apply to Huntress agent",
        "legacyNames": ["huntress_tags"],
        "adminOnly": false,
        "required": false,
        "prompt": "Default Huntress tags (comma-separated, optional)"
      }
    ],
    "unchecky": [
      {
        "name": "policy_unchecky",
        "description": "Default Unchecky policy: install, remove, pin, or empty. Tags override this value.",
        "legacyNames": [],
        "adminOnly": false,
        "required": false,
        "default": "pin",
        "valueOptions": ["pin", "install", "remove"],
        "valueFormat": "{value} | uses pin/install/remove (change to activate policy)",
        "prompt": "Default Unchecky policy (install/remove/pin/empty)"
      },
      {
        "name": "policy_unchecky_url",
        "description": "URL to download Unchecky installer. Required for installation. Get installer from https://www.fosshub.com/Unchecky.html and host it yourself.",
        "legacyNames": [],
        "adminOnly": false,
        "required": true,
        "prompt": "URL to your hosted Unchecky installer (required)"
      }
    ],
    "technician_alerts": [
      {
        "name": "coolforge_technician_alerts",
        "description": "JSON array of pending alerts for technicians (managed by scripts). Technician workstations are identified by the technician emoji tag.",
        "legacyNames": [],
        "adminOnly": false,
        "required": false,
        "autoCreate": true
      }
    ],
    "debugging": [
      {
        "name": "debug_scripts",
        "description": "Debug output level for COOLForge scripts. 'normal' (or empty/false): basics only. 'verbose' (or true/1): policy decisions, actions. 'veryverbose' (or 2): full trace with API calls, variable dumps.",
        "legacyNames": [],
        "adminOnly": false,
        "required": false,
        "default": "normal",
        "valueOptions": ["normal", "verbose", "veryverbose"],
        "valueFormat": "{value} | normal=basics, verbose=debug, veryverbose=full trace",
        "prompt": "Debug output level (normal/verbose/veryverbose)"
      }
    ],
    "policy_readme": [
      {
        "name": "policy_0_readme",
        "description": "Documentation for COOLForge policy system. This field explains how policy_ fields work.",
        "legacyNames": [],
        "adminOnly": false,
        "required": false,
        "readOnly": true,
        "default": "COOLForge Policies | Format: value | docs. Values: pin/install/remove. Tags override. See: github.com/COOLNetworksAU/COOLForge/blob/main/docs/POLICY-FIELDS.md"
      }
    ],
    "device_blocking": [
      {
        "name": "policy_block_device",
        "description": "Block all policy changes on this device. Default 'block' prevents changes until explicitly unblocked. Set to 'unblock' or 'allow' to enable policy enforcement.",
        "legacyNames": [],
        "adminOnly": false,
        "required": false,
        "default": "block",
        "valueOptions": ["block", "unblock"],
        "valueFormat": "{value} | uses block/unblock (change to enable/disable policies)",
        "prompt": "Block policy changes on this device? (block/unblock)"
      }
    ],
    "chrome": [
      {
        "name": "policy_chrome",
        "description": "Chrome Enterprise software policy: install, remove, pin. Uses 5-tag model.",
        "legacyNames": [],
        "adminOnly": false,
        "required": false,
        "default": "pin",
        "valueOptions": ["pin", "install", "remove"],
        "valueFormat": "{value} | uses pin/install/remove (change to activate policy)",
        "prompt": "Chrome software policy (install/remove/pin)"
      }
    ],
    "meshcentral": [
      {
        "name": "policy_meshcentral",
        "description": "MeshCentral agent policy: install, remove, pin. Uses 5-tag model.",
        "legacyNames": [],
        "adminOnly": false,
        "required": false,
        "default": "pin",
        "valueOptions": ["pin", "install", "remove"],
        "valueFormat": "{value} | uses pin/install/remove (change to activate policy)",
        "prompt": "MeshCentral policy (install/remove/pin)"
      },
      {
        "name": "policy_meshcentral_server_url",
        "description": "MeshCentral server URL (e.g., mc.yourcompany.com)",
        "legacyNames": [],
        "adminOnly": false,
        "required": false,
        "prompt": "MeshCentral server URL"
      },
      {
        "name": "policy_meshcentral_download_url",
        "description": "URL to MeshCentral Windows agent installer",
        "legacyNames": [],
        "adminOnly": false,
        "required": false,
        "prompt": "MeshCentral Windows installer URL"
      },
      {
        "name": "policy_meshcentral_linux_install",
        "description": "MeshCentral Linux install command",
        "legacyNames": [],
        "adminOnly": false,
        "required": false
      },
      {
        "name": "policy_meshcentral_mac_download_url",
        "description": "URL to MeshCentral Mac agent installer",
        "legacyNames": [],
        "adminOnly": false,
        "required": false
      }
    ],
    "windows_config": [
      {
        "name": "policy_device_locationservices",
        "description": "Windows Location Services policy: install (enable), remove (disable), pin. Uses 5-tag model.",
        "legacyNames": [],
        "adminOnly": false,
        "required": false,
        "default": "pin",
        "valueOptions": ["pin", "install", "remove"],
        "valueFormat": "{value} | uses pin/install/remove (install=enable, remove=disable)",
        "prompt": "Windows Location Services policy (install=enable/remove=disable/pin)"
      }
    ],
    "chrome_config": [
      {
        "name": "policy_chrome_locationservices",
        "description": "Chrome geolocation policy: install (allow prompts), remove (block), pin. Auto-enables Windows location if needed. Uses 5-tag model.",
        "legacyNames": [],
        "adminOnly": false,
        "required": false,
        "default": "pin",
        "valueOptions": ["pin", "install", "remove"],
        "valueFormat": "{value} | uses pin/install/remove (install=allow, remove=block)",
        "prompt": "Chrome location policy (install=allow/remove=block/pin)"
      }
    ],
    "dnsfilter": [
      {
        "name": "policy_dnsfilter",
        "description": "DNSFilter agent policy: install, remove, pin. Uses 5-tag model.",
        "legacyNames": [],
        "adminOnly": false,
        "required": false,
        "default": "pin",
        "valueOptions": ["pin", "install", "remove"],
        "valueFormat": "{value} | uses pin/install/remove (change to activate policy)",
        "prompt": "DNSFilter policy (install/remove/pin)"
      },
      {
        "name": "policy_dnsfilter_sitekey",
        "description": "DNSFilter site key (NKEY) for agent installation. Get this from your DNSFilter portal.",
        "legacyNames": [],
        "adminOnly": true,
        "required": false,
        "prompt": "DNSFilter site key (NKEY from portal)"
      }
    ]
  }
}
